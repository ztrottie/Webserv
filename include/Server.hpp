#pragma once

class Server
{
private:
	
public:
	// Constructors / Destructor
	Server();
	Server(const Server &inst);
	~Server();

	// Operator Overload
	Server& operator=(const Server &rhs);

	// Functions

};

// Server_HPP