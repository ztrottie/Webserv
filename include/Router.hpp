#pragma once

class Router
{
private:
	
public:
	// Constructors / Destructor
	Router();
	Router(const Router &inst);
	~Router();

	// Operator Overload
	Router& operator=(const Router &rhs);

	// Functions
	
};
