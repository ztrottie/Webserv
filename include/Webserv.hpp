#pragma once

class Webserv
{
private:
	
public:
	// Constructors / Destructor
	Webserv();
	Webserv(const Webserv &inst);
	~Webserv();

	// Operator Overload
	Webserv& operator=(const Webserv &rhs);

	// Functions

};

// Webserv_HPP