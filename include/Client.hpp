#pragma once

class Client
{
private:

public:
	// Constructors / Destructor
	Client();
	Client(const Client &inst);
	~Client();

	// Operator Overload
	Client& operator=(const Client &rhs);

	// Functions

};

// Client_HPP